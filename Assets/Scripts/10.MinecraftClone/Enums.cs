public enum BlockType
{
    GRASS,
    DIRT,
    STONE,
    BEDROCK,
    REDSTONE,
    DIAMOND,
    AIR
}

public enum CubeSide
{
    BOTTOM,
    TOP,
    LEFT,
    RIGHT,
    FRONT,
    BACK
}

public enum ChunkStatus
{
    DRAW,
    DONE,
    KEEP
}